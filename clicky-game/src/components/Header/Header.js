import React from 'react';
import "./Header.css";

const Header = props => (
	<header>
		<div className="Header">
			<h1 className="Header-note">Try to only click once on the same image</h1>
		</div>
	</header>
);

export default Header;